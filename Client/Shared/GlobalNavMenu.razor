@attribute [Authorize(Roles = "Administrator, Operator")]

<MudNavGroup Title="Management" Icon="@Icons.Filled.AdminPanelSettings" Expanded=ExpandedGlobalData>
	
	<MudNavLink Href="UserListPage" Icon="@Icons.Filled.AccountCircle">Users</MudNavLink>
	<MudNavLink Href="ColumnsMainGrid" Icon="@Icons.Filled.Collections">Colums</MudNavLink>
	<MudNavLink Href="RoleListPage" Icon="@Icons.Filled.CameraRoll">User roles</MudNavLink>
	<MudNavLink Href="UserLogPage" Icon="@Icons.Filled.LocalGroceryStore">User log</MudNavLink>
	<MudNavLink Href="OperationLogPage" Icon="@Icons.Filled.Pages">Operation log</MudNavLink>
	<MudNavLink Href="ErroListpage" Icon="@Icons.Filled.ManageHistory">Aplication Errors</MudNavLink>
	
</MudNavGroup>

@code {
	private bool ExpandedGlobalData;
	[CascadingParameter]
	public Task<AuthenticationState> AuthState { get; set; }


	protected async override void  OnInitialized()
	{

		var authState = await AuthState;
		var user = authState.User;

	}

}