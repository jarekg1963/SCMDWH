@attribute [Authorize(Roles = "Administrator, Operator")]

<MudNavGroup Title="Management" Icon="@Icons.Material.Filled.AdminPanelSettings" Expanded=ExpandedGlobalData>
	
	<MudNavLink Href="UserListPage" Icon="@Icons.Material.Filled.AccountCircle">Users</MudNavLink>
	<MudNavLink Href="ColumnsMainGrid" Icon="@Icons.Material.Filled.Collections">Colums</MudNavLink>
	<MudNavLink Href="RoleListPage" Icon="@Icons.Material.Filled.CameraRoll">User roles</MudNavLink>
	<MudNavLink Href="UserLogPage" Icon="@Icons.Material.Filled.LocalGroceryStore">User log</MudNavLink>
	<MudNavLink Href="OperationLogPage" Icon="@Icons.Material.Filled.Pages">Operation log</MudNavLink>
	<MudNavLink Href="ErroListpage" Icon="@Icons.Material.Filled.ManageHistory">Aplication Errors</MudNavLink>
	
</MudNavGroup>

@code {
	private bool ExpandedGlobalData;
	[CascadingParameter]
	public Task<AuthenticationState> AuthState { get; set; }


	protected async override void  OnInitialized()
	{

		var authState = await AuthState;
		var user = authState.User;

	}

}