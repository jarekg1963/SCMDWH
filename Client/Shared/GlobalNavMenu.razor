@attribute [Authorize(Roles = "Administrator, Operator")]

<MudNavGroup Title="Management" Icon="@Icons.Filled.AdminPanelSettings" Expanded=ExpandedGlobalData>
	
	<MudNavLink Href="UserListPage" Icon="@Icons.Filled.AccountCircle">Users</MudNavLink>
	<MudNavLink Href="ColumnsMainGrid" Icon="@Icons.Filled.AccountCircle">Colums</MudNavLink>
	<MudNavLink Href="RoleListPage" Icon="@Icons.Filled.ManageAccounts">User roles</MudNavLink>
	<MudNavLink Href="UserLogPage" Icon="@Icons.Filled.ManageAccounts">User log</MudNavLink>
	<MudNavLink Href="OperationLogPage" Icon="@Icons.Filled.ManageAccounts">Operation log</MudNavLink>
	<MudNavLink Href="ErroListpage" Icon="@Icons.Filled.ManageAccounts">Aplication Errors</MudNavLink>
	
</MudNavGroup>

@code {
	private bool ExpandedGlobalData;
	[CascadingParameter]
	public Task<AuthenticationState> AuthState { get; set; }


	protected async override void  OnInitialized()
	{

		var authState = await AuthState;
		var user = authState.User;

	}

}